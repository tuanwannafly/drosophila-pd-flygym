# Reproducibility Records

The policy files in this directory describe the declared environment and
recording requirements. Artifact-specific provenance remains with the evidence
JSON, report manifest, or campaign manifest that produced it.
