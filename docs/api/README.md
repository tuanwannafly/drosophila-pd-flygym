# API Preparation

This directory documents future service boundaries only. It does not contain
an HTTP server and does not change the Python or browser public APIs.

- [REST endpoint map](rest_api.md)
- [Viewer pose schema](viewer_pose.schema.json)

All analysis, statistics, and validation endpoints are specifications for a
future service layer. They must consume imported artifacts and must not create
scientific evidence implicitly.
