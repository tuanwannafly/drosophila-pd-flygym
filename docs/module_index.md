# Module Index

The complete generated module inventory is preserved in
`docs/release_candidate/module_index.json` and the Python/JavaScript export
inventory in `docs/release_candidate/api_index.json`.

For a human-readable map, see [repository_architecture.md](repository_architecture.md).
For intended reuse surfaces, see [public_api.md](public_api.md).

Primary Python areas:

`analysis`, `anatomy`, `assays`, `automation`, `behavior_platform`,
`controllers`, `digital_twin_platform`, `experiment`, `experiments`, `flystudio`,
`metrics`, `parkinson`, `perturbations`, `research_campaign`,
`research_pipeline`, and `scientific_validation`.
