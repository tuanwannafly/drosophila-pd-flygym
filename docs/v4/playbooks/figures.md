# Figures Playbook

1. Start from a validated machine-readable report.
2. Use deterministic figure code and fixed labels/units.
3. Record evidence/configuration dependencies in a figure manifest.
4. Check captions, legends, axis units, finite values, and source hashes.
5. Export only the approved formats and never overwrite frozen figures.
