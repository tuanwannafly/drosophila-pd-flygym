# Fly Studio Exchange Format

The `.flystudio` package format is a portable, ZIP-based container designed to capture the entire state of a digital fly studio session.
It enables seamless transfer of project data between the Python simulation backend, the Web Viewer, and any future Desktop Viewer.
