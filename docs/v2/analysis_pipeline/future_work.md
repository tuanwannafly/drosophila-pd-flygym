# Future Work

- Add worker-backed parallel batch scheduling.
- Add streaming rollout readers and chunked quality checks.
- Add explicit experiment-level and body-part grouping metadata to normalized feature bundles.
- Add persistent cache manifests and reproducibility hashes.
- Add schema-versioned pipeline configuration files.

These are software extensions only and do not create new biological claims.
