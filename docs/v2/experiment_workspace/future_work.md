# Future Work

- Add a browser-level test harness for panel interaction and multi-condition comparison.
- Add chunked rollout storage and worker-backed analytics for very large datasets.
- Add configurable custom filters with a documented expression language.
- Add synchronized comparison rendering to the existing viewport and chart surfaces.
- Add a server-independent PDF renderer if deterministic PDF bytes are required.

These are software extensions only. They do not change the frozen computational candidate or imply biological Parkinson disease validation.
