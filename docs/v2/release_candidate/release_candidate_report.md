# Release Candidate Report

The release candidate builder emits `release.json`, `release.md`, `release.html`,
module/API indexes, dependency graph, plugin registry, health, validation,
coverage and performance summaries. Reports are inventory artifacts and do not
upgrade the scientific scope.
