# Regression Review

The integration is additive. Existing loaders, workspace, viewer, analysis,
statistics, report and plugin APIs remain the source of behavior. The dashboard
adapter delegates to those services rather than duplicating their state.
