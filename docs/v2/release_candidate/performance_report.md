# Performance Report

Performance is caller-supplied and opt-in. Existing benchmark primitives expose
timing stages and the web verification suite can benchmark only real imported
rollouts. No synthetic workload or simulation is executed by the release gate.
