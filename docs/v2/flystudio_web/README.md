# Fly Studio Web Platform

The web platform is the final user-facing application for the digital fly studio. It consumes the underlying Python components (Viewer, Scene Graph, Playback Engine, etc.) exported as JSON files and visualizes them entirely in the browser using pure HTML/JS/CSS without requiring heavy 3D rendering dependencies.
