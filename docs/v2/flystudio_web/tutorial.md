# Tutorial

1. Navigate to the `web` directory.
2. Launch a local web server (e.g. `python -m http.server`).
3. Open the browser to view the application scaffold.
4. Use the Toolbar to load an exported `session.json` generated by `ViewerSerializer`.
