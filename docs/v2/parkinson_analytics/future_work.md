# Future Work

- Validate additional feature channels against fresh rollout packages without changing frozen reports.
- Add worker-backed extraction and chunked streaming for very large datasets.
- Add browser-level visualization tests and accessibility checks.
- Add provenance manifests for user-supplied score configurations.
- Consider domain-specific gait features only when the required raw observations are available.

These are software and measurement extensions. They are not biological validation claims.
