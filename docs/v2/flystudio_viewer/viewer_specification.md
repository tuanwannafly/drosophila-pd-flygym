# Viewer Specification

Layout definitions must adhere to `single`, `horizontal_split`, `vertical_split`, or `quad`.
Viewer properties are fully JSON serializable via `ViewerSerializer`. This ensures UI state is reproducible across loads.
