# Hướng dẫn tái lập

Dùng commit và environment đã ghi, import đúng rollout, chạy analysis/validation
trên cùng config, rồi so sánh manifest/hash. Release candidate builder chỉ lập
report phần mềm; nó không tái chạy simulation hay tái tạo evidence.
