# Kiểm Định Pipeline

Pipeline validation kiểm tra dependency graph, dataset tồn tại và hash khớp,
reference paths, manifest consistency, publication assets và package contents.
PASS ở đây là tính toàn vẹn phần mềm/artifact, không phải biological validation
hay Parkinson's disease validation.
