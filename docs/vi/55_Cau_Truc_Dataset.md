# 55. Cau truc dataset

Registry su dung cac nhom:

```text
datasets/
  healthy/ pd/ candidate/ control/ validation/ benchmark/
  archive/ incoming/ processed/ failed/
```

Moi dataset co the co `manifest.json`, `checksum.json` hoac
`checksums.sha256`, `metadata/`, va cac file rollout do manifest khai bao.
Registry khong tao payload rong va khong coi thu muc rong la dataset hop le.
