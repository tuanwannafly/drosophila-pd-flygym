# 69. Project Health Monitor

Project Health Monitor gọi các static checks hiện có để kiểm tra module thiếu,
module trùng, import/export heuristic, plugin, dependency cycle, config và
documentation coverage.

Kết quả health là thông tin software/release. Các trạng thái `INFO` không được
diễn giải thành lỗi khoa học.
