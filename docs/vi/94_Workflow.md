# Workflow

Workflow chuẩn là: import reference path vào Dataset Catalog, tạo/queue
campaign, dùng stage adapter cho output hiện có, ghi statistics và validation,
đăng ký figures/reports, xây publication manifest, ghi `study.json`, kiểm tra
hash/reference rồi đóng research package.
