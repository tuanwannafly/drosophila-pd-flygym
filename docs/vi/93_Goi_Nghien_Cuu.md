# Gói Nghiên Cứu

`research_package.zip` chứa `study.json`, `README.md`, `reports/`,
`validation/`, `figures/`, `tables/`, `publication/`, `artifacts/` và
`checksums/`. Gói chỉ đóng lại artifact đã có; không tạo rollout hoặc kết quả
khoa học mới.
