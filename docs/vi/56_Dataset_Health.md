# 56. Dataset health

Bao cao health gom:

- `overall_pass` va `status`;
- schema va metadata;
- file thieu va checksum sai;
- duplicate rollout;
- trajectory, frame, timestamp;
- JSON hong va loi payload.

Bao cao co the xuat thanh JSON/Markdown cung inventory CSV va file SHA-256
thong qua `DatasetRegistry.write_artifacts`.
