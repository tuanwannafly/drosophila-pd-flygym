# Experiment Matrix

The canonical matrix is
`research/campaigns/healthy_baseline/experiment_matrix.csv`.

It contains `Healthy_001` through `Healthy_100`, seeds `0` through `99`, the
existing healthy-baseline configuration, `PLANNED` status, expected output
categories, computational reproducibility validation, and publication targets.
The CSV is a plan, not an observation table.
