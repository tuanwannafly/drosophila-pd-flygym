# Publication Assets

The planning layout is
`research/publication/healthy_baseline/` with `figures`, `tables`, `methods`,
`results`, `discussion`, `supplementary`, `references`, and `assets` folders.

These are empty anchors. Asset generation begins only after real Healthy
rollouts, validation, review, and provenance capture exist.
