# Execution Plan

The future authorized campaign follows:

```text
Import -> Campaign -> Analysis -> Statistics -> Validation -> Publication -> Bundle -> Archive
```

Before execution, freeze commit, configuration, seed, duration, timestep,
environment, output paths, and pass semantics. Every result receives a new
manifest and checksum. No step may overwrite frozen evidence or create a
biological interpretation.
