# Healthy Baseline

Experimental Campaign 01 prepares 100 planned unperturbed computational
experiments using the existing `configs/experiments/healthy_baseline.yaml`
pipeline. The package is planning-only and does not create rollout data.

The dataset contract and templates are under
`research/campaigns/healthy_baseline/`. The Healthy baseline is a computational
simulation baseline, not biological validation.
