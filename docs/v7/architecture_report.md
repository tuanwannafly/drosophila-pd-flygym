# Dataset Integration Architecture Report

The V7 adapter is additive and read-only. It uses the existing V4 dataset
contract and leaves the V6 execution and existing research pipeline APIs
responsible for orchestration.
