# Architecture Report

The implementation consists of a kernel context, event bus, service registry,
resource manager, dependency scheduler, and CLI. Scientific modules remain
outside the V9 implementation boundary.
