# Service Registry

`ServiceRegistry` records the existing subsystem APIs that the kernel may use.
Dataset Adapter, Experiment Runtime, Campaign, and Study are bound to existing
repository objects. Digital Twin, Analysis, Statistics, PD, Validation, and
Publication entries remain explicitly unavailable until an existing public API
is provided; V9 does not fabricate bindings.
