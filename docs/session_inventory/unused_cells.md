# Historical Logic Not Carried Forward

This list identifies notebook logic that was inspected but was not copied
into canonical source. The cells and their stored outputs remain in the
historical notebooks. "Unused" means unused by the current implementation,
not deleted from the research record.

## Session 01

- Cells 12-13: empty-world lookup before a fly is registered. This is a
  Colab sanity probe with no frozen scientific invariant.
- Cells 20-23: repeated print-oriented validation after the complete build.
  The count checks that mattered were narrowed into structured Milestone C
  and anatomy evidence.
- Cells 24-27: Warp/CUDA installation and GPU readiness checks. GPU support is
  optional for rendering and is not required for numerical baseline validity.
- Cell 28: Google Drive mounting and directory creation. This is notebook
  workspace management, not project logic.
- Embedded image cell 19: a historical screenshot, not a canonical figure or
  evidence artifact.

## Session 02

- Cells 8-9 and the repeated API/setup checks around cells 7-11: attempts to
  reuse Session 01 runtime state. Canonical runs construct fresh state and
  emit provenance explicitly.
- Cells 15-18: Rotation3D discovery, failed invocation variants, and repeated
  attachment registry inspection. The verified attachment call is reusable;
  the discovery/retry sequence is not.
- Cells 21, 24, 27, 29, 31, 33, and 39: ground-sensor and compile probes.
  These explain FlyGym attachment behavior, but they are not a substitute for
  the structured Milestone C report.
- Cells 43-47: direct MjSpec/MjsBody navigation experiments, including API
  calls that failed or returned incomplete views. The source/API facts were
  retained only where needed by the compact audits.
- Cells 56, 59, and 61: recover/recreate/environment-reset cycles. These are
  debugging recovery steps, not independent scientific operations.
- Cells 63-65: source-print and axis-discovery formatting around the final
  Block 8.12 audit. The verified 204-DOF and axis-order invariants are
  canonical; the notebook presentation is not.
- Repeated structural summaries in cells 70, 72, 80, 82, and 84: the later
  canonical Milestone 8B implementation consolidated them into pre-state,
  explicit materialization, and post-state checks.

## Historical Cells Retained as Evidence

Blocks 8.12 and 8.13 are not classified as discarded. Their outputs document
the discovery and read-only boundary that led to the canonical audits. Blocks
8.14-8.19 are also retained as historical records, but their repeated
read-only checks were superseded by canonical Milestone 8B. None of these
cells was executed during Phase G1.
