# Execution State Report

The state machine has explicit transitions and rejects invalid jumps. The
waiting path is intentionally first-class so missing datasets are not treated
as failures and no empty scientific result is fabricated.
