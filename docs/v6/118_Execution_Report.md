# Execution Report

Each prepared or executed campaign output contains:

- `execution_report.json`
- `execution_report.md`
- `artifact_registry.json`

The report records dataset discovery, ordered stages, state history, artifact
records, validation returned by the existing pipeline, duration, warnings, and
errors. A waiting report is a valid operational result: it documents why no
scientific work was started.
