# 133. Healthy Dataset Contract

The authoritative Project A contract is
[`research/campaigns/healthy_baseline/healthy_dataset_contract.md`](../../research/campaigns/healthy_baseline/healthy_dataset_contract.md).
It defines the future manifest, metadata, trajectory, checksum, version, and
integrity requirements for `datasets/healthy/<version>/`.

The package is planning-only. No rollout is created here. A ready status means
manifest-backed software/data integrity, not biological validation.
