# Manual Checklist

- [x] Empty repository returns `WAITING_DATASET`.
- [x] Required persistence files are created.
- [x] Waiting execution does not start the pipeline.
- [x] Structural ready binding emits lifecycle events.
- [ ] Add an approved real dataset and confirm `READY` from V7.
- [ ] Run the existing StudyOrchestrator through `experiment_runtime.py run`.
- [ ] Review the generated research package and provenance before scientific use.

The unchecked steps require real curated rollout data and were not performed.
