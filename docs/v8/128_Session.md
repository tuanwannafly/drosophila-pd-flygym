# Session

An `ExperimentSession` stores experiment identity, session identity, state,
bound dataset IDs, campaign metadata, timestamps, duration, warnings, and
errors. It does not store rollout arrays or scientific metrics.

The persisted session file is `session.json`.
