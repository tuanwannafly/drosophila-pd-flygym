# Execution Summary

The summary records dataset discovery, campaign metadata, duration, stage
states, downstream validation output, registered artifacts, and warnings. A
waiting summary is a valid operational result documenting that no experiment
was started.
