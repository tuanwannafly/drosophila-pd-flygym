# Session Report

Session identity, state, event history, dataset binding, and runtime paths are
persisted independently from scientific outputs. Structural lifecycle tests
cover both waiting and ready binding behavior.
