# Execution Events

The runtime emits these events:

`DATASET_READY`, `SESSION_CREATED`, `PIPELINE_STARTED`,
`PIPELINE_COMPLETED`, `VALIDATION_COMPLETED`, `PACKAGE_CREATED`, `FAILED`,
and `WAITING_DATASET`.

Events are ordered, timestamped, and persisted in `execution.json`.
