# Campaigns

Store reviewed campaign definitions and manifests here. A campaign definition
must identify its inputs, configuration, seeds, provenance, expected outputs,
and validation status. Do not place raw rollout data in this directory.
