# Experiment configuration

`job.json` is a metadata template only. Replace it with a real experiment
configuration and provide all seven stage handlers to
`scripts/run_experiment.py`. The template does not create or describe a
scientific rollout.
