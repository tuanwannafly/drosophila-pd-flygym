# Methods

## Planned scope

This file is a publication skeleton for the planned Healthy baseline study.
It will be completed only after an approved rollout package, manifest, and
provenance record exist.

## Dataset and protocol

Record the dataset version, experiment IDs, seeds, configuration, environment,
trajectory observables, and integrity checks here. Do not write values from
memory; link each value to a completed manifest or report.

## Analysis

Record only the existing locomotion, trajectory, behavior, validation, and
report procedures used by the approved execution. Preserve units,
denominators, missingness, and computational scope.

## Reproducibility

Record source commit, configuration hashes, software versions, seeds, output
hashes, and the exact command used after execution is authorized.
